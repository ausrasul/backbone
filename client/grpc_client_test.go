package backbone

/*
import (
	"testing"

	"github.com/stretchr/testify/assert"
)
*/
/*
	- instantiated with server address and port
	- setOnConnect and onDisconnect
	- connects to server by calling openComm
	- sends commands to server
	- add commands handlers
	- receives commands from server.
*/

/*func TestInstantiateClient(t *testing.T) {
	testData := []struct {
		ip   string
		port int
	}{
		{ip: "localhost", port: 12345},
		{ip: "localhost1", port: 123456},
	}
	for _, data := range testData {
		Client := New(data.ip, data.port)
		assert.Equal(t, Server.ip, data.ip, "Ip not set")
		assert.Equal(t, Server.port, data.port, "Port not set")
	}
}*/
